<nav class="pagination" aria-label="Article pages">
    <div>{{#if prev}}<a href="{{page_url prev}}" rel="prev">← Newer articles</a>{{/if}}</div>
    <span>Page {{page}} of {{pages}}</span>
    <div>{{#if next}}<a href="{{page_url next}}" rel="next">Older articles →</a>{{/if}}</div>
</nav>
