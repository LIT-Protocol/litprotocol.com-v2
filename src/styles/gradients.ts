import colors from "./colors";

const gradients ={
    primary: '',
    blog: `linear-gradient(to bottom, ${colors.orange} 80%, ${colors.orange} 25%)`,
    orange: '',
    yellow: '',
    blue: '',
    features: '',
    sunset: '',
    tab: ''
}

export default gradients;